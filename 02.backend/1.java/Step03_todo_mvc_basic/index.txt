Step02에서의 한계점
1. 메인 화면에 해당하는 App.java에서 데이터 입출력, 결과 화면 출력, 연산 등을 전부 수행하고 있음
-> Godclass

Step03
1. 읽어들인 데이터를 parsing해주는 코드를 별도의 클래스인 TodoTSVParsr.java로 분리
2. 분리한 클래스가 위치한 패키지명은 data, data 접근 역할에 해당하는 부분이기 때문에
3. App.java에서 조회(결과값 출력)에 해당하는 코드도 별도의 메서드로 분리

개선된 부분
1. 데이터를 파싱하는 부분을 별도의 클래스로 관심사(역할)을 분리함으로써, 관리가 용이해짐

한계점
1. 데이터 파싱 부분을 별도의 클래스로 분리하였지만, 여전히 App.java에서 출력하는 부분(View),
연산 처리(id값에 따라 특정 Todo 데이터 조회) 등을 전부 수행하고 있음.